# Sangol

This repository contains the deployable Sangol services:

- `backend`: Express + TypeScript API server
- `front`: public Vite/React website
- `admin`: Vite/React admin dashboard

Runtime secrets are intentionally excluded. Copy each `.env.example` file to `.env` locally and fill in environment-specific values.
